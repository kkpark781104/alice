package com.aliceFrame.web.contoller.common;

import java.util.Locale;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

@Controller
public class LoginController {
	/*
	 * @RequestMapping(value = "/login", method = {RequestMethod.GET,
	 * RequestMethod.POST}) public String login(Locale locale, Model model) { String
	 * k=""; return "login/login"; }
	 * 
	 * @RequestMapping(value = "/accessError", method = {RequestMethod.GET,
	 * RequestMethod.POST}) public String accessError(Locale locale, Model model) {
	 * String k=""; model.addAttribute("msg", "접근권한이 없습니다.");
	 * 
	 * return "login/accessError"; }
	 */
}
